{
  "name": "quafu-lesson1-static",
  "version": "0.2.0",
  "private": true,
  "description": "Dependency-light static tutorial and validation guardrails for Quafu Lesson 1.",
  "scripts": {
    "lint": "node scripts/validate.mjs",
    "test": "./scripts/smoke-local.sh",
    "typecheck": "node -e \"console.log('PASS: no TypeScript sources; static HTML/CSS/JS project')\"",
    "validate": "npm run lint && npm run typecheck && npm test"
  }
}
