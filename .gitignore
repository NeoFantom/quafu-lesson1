# OMX runtime state/logs (keep context snapshots versioned)
.omx/*
!.omx/context/
!.omx/context/**

# local runtime artifacts
.DS_Store
node_modules/
.cache/
.tmp/
# Raw browser captures may include account-specific UI. Public pages only use
# sanitized copies under site/assets/screenshots/.
sources/raw/platform-screens/
